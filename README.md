# Initialize Git
git init

# Add files
git add .

# Commit files
git commit -m "Initial commit: Add random letter generator and README"

# Add GitHub remote (replace YOUR_USERNAME with your GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/RandomLetterGenerator.git

# Set main branch
git branch -M main

# Push to GitHub
git push -u origin main
